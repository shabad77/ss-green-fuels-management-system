generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Supplier {
  id        Int      @id @default(autoincrement())
  name      String
  mobile    String
  village   String
  gst       String?
  createdAt DateTime @default(now())
}